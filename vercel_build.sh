#!/bin/bash

# Install Python dependencies
pip install -r requirements.txt

# Create necessary directories
mkdir -p static

# The build command (if you have any build steps, add them here)
echo "Build completed"
